import React from 'react';

// Temporary placeholder component for AboutSection
// This is a workaround for any potential references that might still exist
const AboutSection = () => {
  return null; // This component won't render anything
};

export default AboutSection; 